# Knowledge Graph Cluster

This cluster owns the graph representation and filesystem memory layout.

An expert for this cluster should understand graph nodes, graph edges,
clusters, Markdown documents, YAML manifests, traversal, partitioning by K, and
how graph structure influences retrieval and expert specialization.
