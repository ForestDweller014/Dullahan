# Knowledge Graph Memory

Knowledge Graph Memory represents entities as nodes and relationships as edges.
Nodes can represent files, classes, concepts, images, services, or experts.
Edges capture imports, references, ownership, semantic relationships, and
runtime connections.

Each node and cluster can point to Markdown documents that describe its local
context. Those documents become retrieval targets for CAL and role context for
experts.
